<div class="max-w-4xl mx-auto">
  <div class="mb-6">
    <%= link_to '← Volver a Contenidos', site_admin_contents_path, class: 'text-blue-600 hover:text-blue-800' %>
  </div>

  <div class="bg-white rounded-lg shadow overflow-hidden">
    <div class="px-6 py-4 border-b border-gray-200">
      <h1 class="text-2xl font-bold text-gray-900">Detalles del contenido web</h1>
    </div>

    <div class="p-6">
      <dl class="grid grid-cols-1 gap-x-4 gap-y-6">
        <div>
          <dt class="text-sm font-medium text-gray-500">Clave</dt>
          <dd class="mt-1 text-sm text-gray-900"><%= @content.key %></dd>
        </div>

        <div>
          <dt class="text-sm font-medium text-gray-500">Etiqueta</dt>
          <dd class="mt-1 text-sm text-gray-900"><%= @content.tag %></dd>
        </div>

        <div>
          <dt class="text-sm font-medium text-gray-500">Creado</dt>
          <dd class="mt-1 text-sm text-gray-900"><%= format_date(@content.created_at) %></dd>
        </div>

        <div>
          <dt class="text-sm font-medium text-gray-500">Actualizado</dt>
          <dd class="mt-1 text-sm text-gray-900"><%= format_date(@content.updated_at) %></dd>
        </div>
      </dl>
    </div>
  </div>
</div>
