<div class="max-w-6xl mx-auto">
  <div class="mb-6 flex items-center justify-between">
    <h1 class="text-2xl md:text-3xl font-bold text-gray-900">Editar cliente</h1>
    <%= link_to 'Ver ficha', site_admin_contact_path(@contact), class: 'text-blue-600 hover:text-blue-800 text-sm' %>
  </div>

  <%= render 'form' %>
</div>
